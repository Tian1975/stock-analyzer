name: One-off Backfill Yesterday

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Sync with remote
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run backfill (one-off)
        run: |
          python src/backfill_yesterday.py

      - name: Verify result
        run: |
          echo "Contingut de data/history/scores/:"
          ls -la data/history/scores/

      - name: Commit and push
        run: |
          git add -A data/history/
          git status
          git diff --cached --quiet || git commit -m "One-off: backfill snapshot d'ahir"
          git push origin main
